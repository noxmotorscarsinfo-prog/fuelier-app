
  # Fuelier App

  This is a code bundle for Fuelier App. The original project is available at https://www.figma.com/design/4ZrtORdw5miMgjuuedc43W/Fuelier-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  